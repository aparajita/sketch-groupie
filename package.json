{
  "name": "groupie",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=4.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aparajita/sketch-groupie.git"
  },
  "skpm": {
    "name": "Groupie",
    "manifest": "src/manifest.json",
    "main": "Groupie.sketchplugin"
  },
  "scripts": {
    "build": "skpm-build",
    "link": "skpm-link",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.8.0"
  },
  "dependencies": {
    "copy-webpack-plugin": "^11.0.0",
    "lodash.merge": "^4.6.0"
  }
}
